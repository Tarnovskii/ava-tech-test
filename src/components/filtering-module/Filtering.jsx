import React from 'react'

import s from './filtering.module.css'

const Filtering = props => {
    return (
        <section className={s.additional_windows}>
            Filtering
        </section>
    )
}

export default Filtering